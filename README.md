# engram-rs
Persistent memory engine for AI coding assistants. Hybrid keyword search over SQLite + FTS5. MCP-native (drop-in memory for any MCP host). Works standalone — no API keys, no embeddings, no LLM required.
